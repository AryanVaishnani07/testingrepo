name: my first workflow

on: [push]

jobs:
  build: 
    runs-on: ubuntu-latest

    steps:
      - name: checkout code 
        uses: actions/checkout@v4

      - name : run a script 
        run: echo "CI/CD Pipline is working"
